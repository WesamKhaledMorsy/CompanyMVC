@model IEnumerable<ApplicationUser>
@{
    ViewData["Title"] = "All Users";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

@* <div class="w-100 d-flex align-content-center justify-content-lg-end">
    <button type="button" class="btn btn-primary">
        <a asp-action="Create" class="text-light text-decoration-none">Create Employee</a>
    </button>
</div> *@
<br />
<div class="row justify-content-center algin-items-center mt-1 ">
    <form class="row col-8 offset-2" asp-action="Index">
        <div class=" col-8">
            <input type="text" class="form-control" name="searchValue" placeholder="Search by Email ....." />
        </div>
        <div class="col-4">
            <input type="submit" value="Search" class="btn btn-success" />
        </div>
    </form>
</div>
@if (Model.Count() > 0)
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(U => U.Id)</th>
                <th>@Html.DisplayNameFor(U => U.UserName)</th>
                <th>@Html.DisplayNameFor(U => U.Email)</th>  
                <th>Details</th>
                <th>Update </th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.Id</td>
                   @*  <td>
                        <img src="~/Files/Images/@employee.ImageUrl" height="40" width="40" alt="EmployeeImage" />
                    </td> *@
                    <td>@user.UserName</td>
                    <td>@user.Email</td>
                    <partial name="_ActionButtonPartialView" model="@user.Id.ToString()"></partial>
                </tr>
            }
        </tbody>
    </table>
}
else
{

    <div class="alert alert-warning" role="alert">
        <center>
            There is No USers
        </center>
    </div>
}


